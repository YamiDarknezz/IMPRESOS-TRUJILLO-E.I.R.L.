# Dependencias / entornos
node_modules/
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo

# Build / distribución
dist/
build/
*.egg-info/
.angular/
out-tsc/

# Entornos y secretos
.env
.env.*
# La plantilla SÍ se versiona: documenta qué variables necesita el sistema.
# Sin esta excepción, la regla `.env.*` la deja fuera del repositorio.
!.env.example
*.local
secrets.json
credentials.json

# IDE / SO
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Temporales de edición de docx / xlsx
*_unpacked/
unpacked/
~$*

# Carpeta de archivos temporales del agente (scripts, unpacked, etc.)
.temp/

# Configuración local de opencode
opencode.json

# Órdenes del agente (instrucciones locales, no versionar)
AGENTS.md
